用户正在学习信号与系统和MATLAB仿真，以下要求适用于本次会话里所有 MATLAB 代码请求：
- 兼容 MATLAB R2023b，不使用之后版本独有的函数；
- 只返回完整、可直接运行的单文件代码 + 必要的中文注释，代码之外不附任何解释文字；
- 变量名沿用我每次指定的名称，不要改名；
- 绘图规范：
  · 字号：图中所有文字（坐标轴刻度、xlabel/ylabel、title、legend、text 标注）统一 24 pt，不留默认小字号；
  · 线宽：所有曲线线宽 2；
  · 配色：多条曲线用深色高对比（蓝、红、黑、深绿），并兼用实线/虚线/点线区分，避免浅色（纯绿/青/黄）；
  · 网格：grid on；
  · 图例：仅当同一坐标轴内画有多条曲线时才加 legend 区分；单曲线图（含每个只画一条曲线的子图）不加 legend，其含义由标题与纵轴标签标明；
  · 画布：单幅图用默认尺寸即可；纵向排列的多子图必须按子图数 N 加高、避免过扁，例如先 figure('Position',[100 100 900 350*N]) 再 subplot；
- 不要添加任何测试桩、自检打印或调试残留。


请用 MATLAB 求一个连续 LTI 系统的零输入响应、零状态响应与完全响应，并验证完全响应等于前两者之和。
系统用状态空间模型描述，其中 A = [-1 -1; 4 -6]，B = [1; 0]，C = [-1 0]，D = 1。
系统在 t<0 时输入恒为 2 且已达稳态，并以此作为起始状态；t>=0 时输入为 4。
请按以下步骤实现：
  1. 用 ss 建立系统模型 sys；
  2. 对输入 2 仿真足够长时间（比如 10 秒）使系统达到稳态，取末时刻状态作为起始状态 x0；
  3. 取 t 从 0 到 10 秒，求三个响应并分别存入 izi、izs、ifull：izi 为零输入响应（用 initial、起始状态 x0），izs 为零状态响应（用 lsim、起始状态 0、输入 4），ifull 为完全响应（用 lsim、起始状态 x0、输入 4）；打印 ifull 与 izi+izs 的最大误差；
  4. 作为对照，再求一次完全响应：对一段 t<0 为 2、t>=0 为 4 的输入，自 t 从 -10 到 10、以零起始状态一次仿真得 ifull2；
  5. 把 izi、izs、ifull 与 ifull2 画在同一张图中对比（显示 t 从 -10 到 10），可见 ifull2 在 t>=0 段与完全响应重合。
