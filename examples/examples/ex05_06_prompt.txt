用户正在学习信号与系统和MATLAB仿真，以下要求适用于本次会话里所有 MATLAB 代码请求：
- 兼容 MATLAB R2023b，不使用之后版本独有的函数；
- 只返回完整、可直接运行的单文件代码 + 必要的中文注释，代码之外不附任何解释文字；
- 变量名沿用我每次指定的名称，不要改名；
- 显示符号表达式时用 disp，不要用 pretty；
- 绘图规范：
  · 字号：图中所有文字（坐标轴刻度、xlabel/ylabel、title、legend、text 标注）统一 24 pt，不留默认小字号；
  · 线宽：所有曲线线宽 2；
  · 配色：多条曲线用深色高对比（蓝、红、黑、深绿），并兼用实线/虚线/点线区分，避免浅色（纯绿/青/黄）；
  · 网格：grid on；
  · 图例：仅当同一坐标轴内画有多条曲线时才加 legend 区分；单曲线图（含每个只画一条曲线的子图）不加 legend，其含义由标题与纵轴标签标明；
  · 画布：单幅图用默认尺寸即可；纵向排列的多子图必须按子图数 N 加高、避免过扁，例如先 figure('Position',[100 100 900 350*N]) 再 subplot；
- 不要添加任何测试桩、自检打印或调试残留。


我在学拉普拉斯变换与傅里叶变换的关系——傅里叶变换就是拉普拉斯变换在虚轴 s=jw 上的取值。请用 MATLAB 把下面两例画成清楚漂亮、能一眼看懂“傅里叶就是虚轴上的拉普拉斯”的三维图：
(1) 信号：矩形脉冲 x(t)=1 (0<=t<=T) 其余为 0，X(s)=(1-exp(-s*T))/s；
(2) 系统：一阶系统 H(s)=1/(s+a)，a>0。
每例在 s 平面（横轴 sigma、纵轴 w）画 |.| 的三维曲面 surf，并做到：
- 把虚轴 sigma=0 上的那条切片用一条醒目的粗红线（plot3、线宽3）叠画在曲面上——傅里叶变换就是这条线，要让它在曲面上突出可见；
- 幅度超过曲面主体高度的部分限幅，并用 caxis 让 colormap 配色跨越可见高低、不要整个曲面一种颜色；用 shading interp 加光照（camlight/lighting）增立体感、选一个能看清虚轴那条红线的视角；
- sigma 范围聚焦虚轴附近，看得清结构。
再各配一个二维子图：把这条虚轴切片与频谱 |X(jw)|、频率响应 |H(jw)| 画在一起比较、验证两者重合。
请给出完整可直接运行的 MATLAB 代码（兼容 R2023b，不使用之后版本的新函数），并简要说明思路。
