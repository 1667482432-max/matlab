我在做信号与系统课程的语音合成大作业，下面所有 MATLAB 代码请求都用于这个大作业。以下要求适用于本次会话里所有 MATLAB 代码请求：
- 兼容 MATLAB R2023b，不使用之后版本独有的函数；
- 只返回完整、可直接运行的单文件代码 + 必要的中文注释，代码之外不附任何解释文字；
- 变量名沿用我每次指定的名称，不要改名；
- 显示符号表达式时用 disp，不要用 pretty；
- 绘图规范：
  · 字号：图中所有文字（坐标轴刻度、xlabel/ylabel、title、legend、text 标注）统一 24 pt，不留默认小字号；
  · 线宽：所有曲线线宽 2；
  · 配色：多条曲线用深色高对比（蓝、红、黑、深绿），并兼用实线/虚线/点线区分，避免浅色（纯绿/青/黄）；
  · 网格：grid on；
  · 图例：仅当同一坐标轴内画有多条曲线时才加 legend 区分；单曲线图（含每个只画一条曲线的子图）不加 legend，其含义由标题与纵轴标签标明；
  · 画布：单幅图用默认尺寸即可；纵向排列的多子图必须按子图数 N 加高、避免过扁，例如先 figure('Position',[100 100 900 350*N]) 再 subplot；
- 不要添加任何测试桩、自检打印或调试残留。


提示词卡
我想造一个浊音的激励信号：一串周期性的单位样值脉冲，脉冲之间其余样点为零，脉冲的间隔就是基音周期。
抽样频率 8000 Hz，请先生成一段持续 1 秒、基音频率为 200 Hz 的脉冲串并用 sound 试听，再生成一段 300 Hz 的脉冲串试听，听一听基音不同时音高的区别。
然后再做一段 1 秒的脉冲串，但让基音周期随时间分段变化：每 10 毫秒为一段，第 m 段（m 从 0 数起）的基音周期取 PT = 80 + 5*mod(m,50) 个样本，段内间隔固定、段间不同，同样试听。
