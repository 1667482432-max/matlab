用户正在学习信号与系统和MATLAB仿真，以下要求适用于本次会话里所有 MATLAB 代码请求：
- 兼容 MATLAB R2023b，不使用之后版本独有的函数；
- 只返回完整、可直接运行的单文件代码 + 必要的中文注释，代码之外不附任何解释文字；
- 变量名沿用我每次指定的名称，不要改名；
- 显示符号表达式时用 disp，不要用 pretty；
- 绘图规范：
  · 字号：图中所有文字（坐标轴刻度、xlabel/ylabel、title、legend、text 标注）统一 24 pt，不留默认小字号；
  · 线宽：所有曲线线宽 2；
  · 配色：多条曲线用深色高对比（蓝、红、黑、深绿），并兼用实线/虚线/点线区分，避免浅色（纯绿/青/黄）；
  · 网格：grid on；
  · 图例：仅当同一坐标轴内画有多条曲线时才加 legend 区分；单曲线图（含每个只画一条曲线的子图）不加 legend，其含义由标题与纵轴标签标明；图例须放在不遮挡曲线与数据的位置——优先数据稀疏的角；若绘图区内空白不足，用 legend(...,'Location','northoutside') 置于图外上方、或适当扩大纵轴范围留出空白带，切勿压住主波形；
  · 画布：单幅图用默认尺寸即可；纵向排列的多子图必须按子图数 N 加高、避免过扁，例如先 figure('Position',[100 100 900 350*N]) 再 subplot；
- 不要添加任何测试桩、自检打印或调试残留。


请用 MATLAB 验证因果系统的系统函数实部与虚部之间的约束。
因果系统的冲激响应是 h(t)=exp(-t)（t>=0 取此值、t<0 为 0）。
我有一个已经写好的函数 prefourier，请直接调用、不要重新定义。函数代码如下：
function [t, omg, FT, IFT] = prefourier(Trg, N, OMGrg, K)
T = Trg(2) - Trg(1);
t = linspace(Trg(1), Trg(2)-T/N, N)';
OMG = OMGrg(2) - OMGrg(1);
omg = linspace(OMGrg(1), OMGrg(2)-OMG/K, K)';
FT  = T/N * exp(-1j*(omg*t.'));
IFT = OMG/2/pi/K * exp(1j*(t*omg.'));
end
请先用 prefourier 求系统函数 H(jw)；再由因果系统实部与虚部互为希尔伯特变换的关系，只用 H 的实部重建完整系统函数 H1，并逆变换回冲激响应 h1(t)。
把 h1 与原 h、H1 与原 H 各画在一起对比；画 h1 的时间范围请含一段 t<0（如从 t=-2 起），以便观察重建结果的因果性。
并打印 w=1、w=2 处重建虚部 imag(H1) 的值，以及逆变换 h1 残余虚部的最大模。
