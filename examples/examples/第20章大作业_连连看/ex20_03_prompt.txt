我在做信号与系统课程的连连看大作业（先自己制作连连看，再写程序攻克别人的连连看），下面所有 MATLAB 代码请求都用于这个大作业。以下要求适用于本次会话里所有 MATLAB 代码请求：
- 兼容 MATLAB R2023b，不使用之后版本独有的函数；
- 只返回完整、可直接运行的单文件代码 + 必要的中文注释，代码之外不附任何解释文字；
- 变量名沿用我每次指定的名称，不要改名；
- 显示矩阵或符号表达式时用 disp，不要用 pretty；
- 绘图规范：
  · 字号：图中所有文字（坐标轴刻度、xlabel/ylabel、title、legend、text 标注）统一 24 pt，不留默认小字号；
  · 线宽：所有曲线线宽 2；
  · 配色：多条曲线用深色高对比（蓝、红、黑、深绿），并兼用实线/虚线/点线区分，避免浅色（纯绿/青/黄）；
  · 网格：曲线图 grid on（imagesc/imshow 图像显示可不加网格）；
  · 图例：仅当同一坐标轴内画有多条曲线时才加 legend；单曲线图不加，其含义由标题与轴标注明；图例不得遮挡数据；
  · 画布：单幅图用默认尺寸即可；纵向排列的多子图按子图数 N 加高、避免过扁，例如先 figure('Position',[100 100 900 350*N]) 再 tiledlayout/subplot；
- 不要添加任何测试桩、自检打印或调试残留。


我有一张游戏盘的灰度截图 graygroundtruth.jpg（当前路径，imread 可直接读）。盘面是规整网格、每格一个居中的图案，背景比图案更有规律，横竖方向的平均灰度都有明显的空间周期。
请用傅里叶分析分块：对图像每一列取所有行的平均，得到一条随水平位置变化的平均灰度曲线，它的傅里叶幅度谱主峰对应块宽、主峰处的相位对应横向起始偏移（左侧空白）；竖直方向同理得块高与上方偏移。每块不小于 30 像素，据此认准块对应的主峰、避开图案内部纹理产生的高频峰。按定出的块宽、块高与偏移把整图切成图像单元。
打印块宽、块高与行列数，并显示若干图像单元。图像单元存入元胞数组 blocks。
