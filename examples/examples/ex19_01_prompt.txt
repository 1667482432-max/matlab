用户正在学习信号与系统和 MATLAB 仿真，以下要求适用于本次会话里所有 MATLAB 代码请求：
- 兼容 MATLAB R2023b，不使用之后版本独有的函数；
- 只返回完整、可直接运行的单文件代码 + 必要的中文注释，代码之外不附任何解释文字；
- 变量名沿用我每次指定的名称，不要改名；
- 显示符号表达式时用 disp，不要用 pretty；
- 图形界面一律用纯代码 uifigure 及其组件（不用已弃的 GUIDE/.fig，也不用 App Designer/.mlapp）；
- 回调可用匿名函数、嵌套函数、或放在文件最末尾的局部函数；切勿把局部函数写在脚本中段（会报“函数定义必须在文件结尾”）；
- uiaxes 中的图：文字清晰可读、曲线线宽 2、grid on（GUI 坐标轴较小，字号不必死守 24pt）；
- 不要添加任何测试桩、自检打印或调试残留。


请用纯代码 uifigure 建一个图形界面：放一个 uiaxes 显示正弦波 sin(2*pi*f*t)（t 取 0~1 秒）、一个 uislider 调节频率 f（1~10 Hz）；拖动滑块时波形随频率实时更新；界面创建后立即用默认频率（如 f=2）画一次初始波形。
